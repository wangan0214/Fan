---
更新时间：2026-07-23 00:45:00
版本：v1.0.0 — 个人主页搭建与部署全指南（初版，覆盖 Astro 静态站 + Cloudflare Pages + fangeai.com 全链路 + 安全基线 + 内容更新工作流）
---

# 帆哥个人主页 · 搭建与部署全指南

> 一份可复用的「个人主页从零到上线」完整 SOP。不写代码也能完成，全程免费、免备案、数据自主。

---

## 0. 目标与技术选型

**目标**：做一个类似 `zarazhang.com` 的个人主页——有设计感、用真实自我介绍填充、能自己更新文章。

**关键决策**：

| 决策点 | 选择 | 原因 |
|---|---|---|
| 要不要备案 | **不备案** | 服务器放在境外（Cloudflare），按法规无需 ICP 备案 |
| 技术方案 | **Astro 静态站** | 零运行时、零服务器、SEO 友好、内容用 Markdown 写 |
| 托管 | **Cloudflare Pages** | 境外节点、免备案、带宽无限、免费 |
| 域名 | **国际域名 fangeai.com** | Cloudflare Registrar 成本价 $10.46/年，WHOIS 隐私免费 |

**免备案逻辑**：只要网站文件托管在境外服务器，且不使用境内 ICP 备案服务，就不需要备案。Cloudflare Pages 的节点在境外，天然满足。

---

## 1. 本地搭建（Astro v5 静态站）

项目路径：`/Users/fanshuai/Documents/搞钱集中营/personal-homepage/`

**关键文件**：
- `astro.config.mjs`：`output` 默认 `static`（纯静态，无需 adapter）；`site` 设正式域名；集成 `@astrojs/sitemap`
- `src/layouts/BaseLayout.astro`：全局布局 + 设计系统（CSS 变量、header、footer）
- `src/pages/index.astro`：首页（hero + 板块 + 文章列表）
- `src/pages/about.astro`：关于页（真实简介 + 工具箱 + 精选项目）
- `src/pages/blog/`：博客列表 + 文章详情
- `src/content/blog/*.md`：Markdown 文章

**本地预览**：
```bash
npm install
npm run dev        # http://localhost:4321
npm run build      # 输出到 dist/
```

---

## 2. 设计 v2 视觉升级（参考）

v1 被批「太素、没底色」。v2 设计系统（见 `BaseLayout.astro`）：

- **字体**：衬线风 `Vollkorn`（标题）+ `Gentium Book Basic`（正文），通过 Google Fonts 引入
- **配色**：暖白底 `#f5f0e8` + 深红强调色 `#b84a3c` + 深色 footer `#1a1814`
- **视觉元素**：
  - 毛玻璃 sticky header（`backdrop-filter: blur(8px)`）
  - 径向渐变背景（两层 `radial-gradient`）
  - 深色 footer
  - 卡片化板块（`.card` 带阴影）
  - hero 渐变标题文字（`-webkit-background-clip: text`）
  - 胶囊按钮（`.cta`）

---

## 3. 内容填充（真实简介）

来源：GitHub Profile 中文版 + 飞书 wiki 自我介绍。

- 首页 tagline：**「不会写代码的售前，用 AI Agent 把想法变成产品。」**
- 关于页：十年 B2B SaaS 售前、AI coding agent 实践、工具箱（Claude / Codex / 扣子 / AI-Media2Doc）、精选项目（AI 自媒体工具箱 / WorkBuddy 技能库 / 售前 AI 提效 / 飞书 AI 自动化）

---

## 4. 部署到 Cloudflare Pages

**创建项目**（⚠️ 关键）：
- 必须创建 **纯 Pages** 项目（连接 Git），**不要**误建成 Worker 类型
- 连接 GitHub 仓库 `wangan0214/Fan`
- 构建命令：`npm run build`
- 输出目录：`dist`

**坑**：早期用浏览器网页上传首版文件时，可能没有触发 Cloudflare 的部署 webhook，导致线上停留在旧版本。正确做法是用 `git push` 触发自动部署。

---

## 5. 域名购买与绑定

1. **Cloudflare Registrar** 购买国际域名 `fangeai.com`（$10.46/年，支持 PayPal/信用卡）
2. Cloudflare Pages → **自定义域** → 输入 `fangeai.com` → **激活**
3. DNS 自动配置 CNAME 指向 Pages，等待生效（约几分钟）
4. 验证：`https://fangeai.com` 可访问

---

## 6. Cloudflare 安全配置（免费版完整基线）

### 6.1 用 API 批量设置（需临时 Token）

生成 **Zone 编辑权限**的 API Token（Cloudflare 控制台 → 我的个人资料 → API 令牌 → 创建自定义令牌，权限：Zone Settings Edit + SSL Certificates Edit + Zone Read，限定 `fangeai.com`）。

通过 API 设置（值已验证）：

| 设置 | API 值 | 说明 |
|---|---|---|
| `ssl` | `"strict"` | 完全（严格）端到端加密 |
| `always_use_https` | `"on"` | 注意值是字符串 "on"，不是布尔 |
| `security_level` | `"medium"` | 中等防护 |
| `min_tls_version` | `"1.2"` | 弃用 TLS 1.0/1.1 |

**免费版 API 不暴露、需 UI 手动设的两项**：
- `hsts` → 报 "Unrecognized zone setting name"
- `bot_management_fight_mode` → 报 "Unrecognized zone setting name"

**用完务必撤销该 Token**（API 令牌页 → 撤销），不留后门。

### 6.2 UI 手动设置（免费版 API 不暴露的部分）

| 设置 | 路径 | 操作 |
|---|---|---|
| HSTS | SSL/TLS → 边缘证书 → Enable HSTS | 开主开关 + Max Age 设 6 months，其余默认关 |
| Bot Fight Mode | 安全性 → 设置 → Filter 切「Bot 流量」 | 打开开关 |
| Under Attack Mode | 安全性 → 设置 | **保持关闭**（紧急 DDoS 才开） |
| AI Labyrinth | 安全性 → 设置 | 保持开启（干扰 AI 训练爬虫） |

> 新版 Cloudflare 已移除传统 Security Level 四档选择器，仅保留 Under Attack Mode 开关；免费版靠 Bot Fight Mode 承担中等防护。

---

## 7. 内容更新工作流（日常）

```
本地改 src/ 下文件  →  git push origin main  →  Cloudflare 自动部署（1-2 分钟）  →  浏览器硬刷新
```

- 改文章：编辑 `src/content/blog/*.md`
- 改页面：编辑 `src/pages/*.astro`
- 改样式：编辑 `src/layouts/BaseLayout.astro`
- 生效时间：push 后约 1-2 分钟
- 浏览器刷新：`Cmd+Shift+R`（Mac）硬刷新，排除缓存

---

## 8. 故障排查：线上不刷新（本次真实案例）

**现象**：本地改了 v2 设计，但 `fangeai.com` 线上还是旧版 v1。

**排查步骤**：
1. `curl -s https://fangeai.com` 抓线上 HTML，对比本地 v2 特征（毛玻璃 header、深色 footer、径向渐变、hero-accent）
2. `WebFetch https://raw.githubusercontent.com/<user>/<repo>/main/src/layouts/BaseLayout.astro` 确认 GitHub 远程是否 v2
3. 结论判断：
   - 本地=v2、远程=v2、线上=v1 → **Cloudflare 部署没刷新**（webhook 未触发）
   - 本地=v2、远程=v1 → 本地改动没 push 上去

**本次根因**：`a730d3c` 空提交没有真正触发 Cloudflare 重新部署，Cloudflare 停留在最早的 v1 构建上。

**修复**：
- 方案 A：Cloudflare 控制台 → Workers 和 Pages → 项目 → 部署 → 重试部署（Redeploy）
- 方案 B：GitHub 推一个空提交 `git commit --allow-empty -m "trigger"` 触发 webhook
- 方案 C：重新生成 API Token，用 API 强制触发部署

---

## 9. 资源清单

| 项目 | 值 |
|---|---|
| GitHub 仓库 | `wangan0214/Fan` |
| 线上域名 | `https://fangeai.com` |
| Cloudflare Zone ID | `360e038a7b015432dc506748138f2bf7` |
| 技术栈 | Astro v5 + Cloudflare Pages（纯静态） |
| 设计版本 | v2（暖色衬线风） |
| 安全基线 | Full Strict + Always HTTPS + Medium + TLS 1.2 + HSTS + Bot Fight Mode |

---

## 10. 免费额度与上限

- 带宽/请求：**无限**
- 构建次数：**500 次/月**（个人站完全够）
- 域名：Registry 成本价，无溢价
- 个人站流量小，免费方案长期可用，无需升级 Pro

---

*文档生成：2026-07-23，沉淀自 fangeai.com 完整搭建与安全配置过程。*
