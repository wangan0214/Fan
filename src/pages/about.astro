---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="关于 | 帆哥" description="不会写代码的售前，用 AI Agent 把想法变成产品">
  <div class="about-page">

    <!-- Hero intro + 头像 -->
    <section class="about-hero">
      <div class="hero-accent"></div>
      <div class="hero-avatar-row">
        <img src="/images/avatar-cartoon.jpeg" alt="帆哥" class="hero-avatar" />
        <div class="hero-text">
          <h1>关于帆哥</h1>
          <p class="lead">
            <strong>不会写代码的售前解决方案专家</strong>，略懂 SaaS 行业的 KA 销售和渠道生态——但在 AI Agent 的帮助下，把一个个想法变成了能用的产品。
          </p>
        </div>
      </div>
    </section>

    <!-- Story -->
    <section class="card">
      <h2>我的故事</h2>
      <p>
        做了十年 B2B SaaS，最熟悉的事是给客户讲方案、做 Demo、搭渠道体系。2025 年 AI coding agent 爆发后，我开始用自然语言"写代码"——不是写 <code>class</code> 和 <code>function</code>，而是描述我想要什么，让 AI 帮我实现。
      </p>
      <p>
        我的核心命题：<em>一个不懂编程的业务人，能不能靠 AI Agent 构建自己的数字产品？</em> 答案是可以。下面是我日常真正在用的工具矩阵。
      </p>
    </section>

    <!-- Toolkit — 完整版 -->
    <section class="card">
      <h2>🛠 我的 AI 工具箱</h2>
      <p class="tool-intro">这些不是"听说好用"的工具，是我每天实际在用的生产力栈。按用途分类：</p>

      <!-- 分类 1: AI 对话与深度思考 -->
      <h3 class="tool-category">💬 对话 & 深度思考</h3>
      <div class="tool-grid">
        <div class="tool-item">
          <strong>Claude</strong>
          <span>深度推理、长文写作、方案架构——我的主力"第二大脑"，擅长复杂分析和结构化输出</span>
        </div>
        <div class="tool-item">
          <strong>ChatGPT</strong>
          <span>通用对话、创意发散、GPT-4o 多模态能力——日常快速问答和头脑风暴的首选</span>
        </div>
        <div class="tool-item">
          <strong>DeepSeek（深度求索）</strong>
          <span>中文语境强、代码能力强、性价比极高——技术调研和中文内容生成的得力助手</span>
        </div>
        <div class="tool-item">
          <strong>Kimi</strong>
          <span>超长上下文窗口（20万+ token）——适合丢整份 PDF/代码仓库进去做分析</span>
        </div>
        <div class="tool-item">
          <strong>豆包（Doubao / 字节跳动）</strong>
          <span>字节系 AI 助手，中文理解优秀、响应快——日常轻量问答和搜索增强</span>
        </div>
        <div class="tool-item">
          <strong>Kun（昆仑万维 Skywork）</strong>
          <span>国产大模型之一，中英双语能力均衡——多模型对比测试时的参考选手</span>
        </div>
      </div>

      <!-- 分类 2: AI 编程与自动化 -->
      <h3 class="tool-category">⚡ 编码 & 自动化</h3>
      <div class="tool-grid">
        <div class="tool-item featured">
          <strong>WorkBuddy</strong>
          <span><b>我的核心工作台。</b>AI Agent 平台，内置文件读写/浏览器/终端/GitHub 连接器。不写代码也能指挥 AI 帮你改项目、跑命令、管文件——本站就是用它搭建的</span>
        </div>
        <div class="tool-item featured">
          <strong>Codex（OpenAI）</strong>
          <span><b>AI 编程引擎。</b>通过自然语言描述直接生成完整代码项目，擅长全栈开发、从零搭建应用。和 WorkBuddy 配合使用：Codex 写代码，WorkBuddy 部署运维</span>
        </div>
        <div class="tool-item">
          <strong>Codex++</strong>
          <span>Codex 的增强封装版本，提供更结构化的工作流模板和项目管理能力</span>
        </div>
        <div class="tool-item">
          <strong>Codex++ 管理工具</strong>
          <span>Codex++ 项目的配套管理面板，用于跟踪多个 AI 编码任务的进度和状态</span>
        </div>
        <div class="tool-item">
          <strong>Cursor / Windsurf（CC Switch）</strong>
          <span>AI 原生 IDE，代码补全+编辑+重构一体化。CC Switch 用于在不同 AI 编辑器间快速切换配置</span>
        </div>
        <div class="tool-item">
          <strong>ZCode</strong>
          <span>轻量级代码编辑/运行环境，用于快速测试代码片段和小型脚本</span>
        </div>
        <div class="tool-item">
          <strong>Docker</strong>
          <span>容器化部署工具——虽然我不写代码，但用 Docker 一键启动各种 AI 工具的服务端环境</span>
        </div>
        <div class="tool-item">
          <strong>Marvis</strong>
          <span>AI 辅助的开发者工具，专注于代码理解和智能提示</span>
        </div>
        <div class="tool-item">
          <strong>MiniMax Code</strong>
          <span>MiniMax 出品的代码生成模型，国产替代路线上的一个选择</span>
        </div>
        <div class="tool-item">
          <strong>EchoBird</strong>
          <span>AI 工具链中的一环，用于特定场景的自动化处理</span>
        </div>
      </div>

      <!-- 分类 3: 智能体与工作流 -->
      <h3 class="tool-category">🤖 智能体 & 工作流</h3>
      <div class="tool-grid">
        <div class="tool-item featured">
          <strong>扣子（Coze / 字节跳动）</strong>
          <span><b>智能体搭建平台。</b>可视化拖拽搭建 ChatBot 和 AI Agent，可一键接入飞书、微信公众号、抖音等平台——我做自媒体自动化的核心节点</span>
        </div>
        <div class="tool-item">
          <strong>CodeBuddy CN</strong>
          <span>CodeBuddy 国内版，针对国内网络环境优化的 AI 编程助手</span>
        </div>
        <div class="tool-item">
          <strong>skills-manager</strong>
          <span>WorkBuddy 的 Skill 管理工具——用于创建、编辑、发布可复用的 AI 工作流技能包</span>
        </div>
      </div>

      <!-- 分类 4: 内容生产 -->
      <h3 class="tool-category">📝 内容生产</h3>
      <div class="tool-grid">
        <div class="tool-item featured">
          <strong>AI-Media2Doc</strong>
          <span><b>口播视频 → 公众号图文一键转换。</b>OBS 录制口播 → AI 转录 → 自动截图排版 → 生成 Markdown/HTML 可直接粘贴到公众号后台。一个人撑起整个内容生产流水线</span>
        </div>
      </div>
    </section>

    <!-- Current work -->
    <section class="card">
      <h2>🚀 我正在做的事</h2>
      <ul class="clean-list">
        <li><strong>自媒体自动化流水线</strong> — OBS 录制口播 → AI 转录截图 → 一键发布微信公众号，一个人完成一个团队的工作</li>
        <li><strong>AI Builder 开发者</strong> — 围绕内容生产、数据分析、客户沟通搭建了一批可复用的 AI 工作流与智能体</li>
        <li><strong>售前 × AI 效率工具</strong> — 用 AI Agent 做方案自动生成、竞品对比、招标分析</li>
        <li><strong>飞书 × AI 深度集成</strong> — 把 AI 能力嵌入日常工作流：审批、报表、客户管理</li>
      </ul>
    </section>

    <!-- Philosophy -->
    <section class="card philosophy">
      <blockquote>
        "代码不再是工程师的特权，而是每个想解决问题的业务人的工具。"
        <cite>—— 帆哥</cite>
      </blockquote>
      <p>
        我不做"技术产品"，我做的是"用技术解决业务问题"的方案。我面向的是那些想用 AI 提升效率、但又不想从零学编程的销售、售前、运营。
      </p>
    </section>

    <!-- QR Code Social Zone -->
    <section class="card qr-zone">
      <h2>📡 找到我</h2>
      <p class="qr-intro">扫码关注 / 加群，一起聊 AI、聊搞钱、聊用 Agent 干活</p>

      <div class="qr-grid">
        <!-- 飞书群 -->
        <div class="qr-card">
          <div class="qr-img-wrap">
            <img src="/images/qr-feishu-group.png" alt="飞书AI粉丝群" loading="lazy" />
          </div>
          <div class="qr-label">
            <strong>📘 飞书 AI 粉丝群</strong>
            <span>帆哥AI粉丝群（01）· 帆友科技</span>
          </div>
        </div>

        <!-- 微信公众号 -->
        <div class="qr-card">
          <div class="qr-img-wrap">
            <img src="/images/qr-wechat-mp.jpg" alt="帆哥AI研究室" loading="lazy" />
          </div>
          <div class="qr-label">
            <strong>💬 微信公众号</strong>
            <span>帆哥AI研究室</span>
          </div>
        </div>

        <!-- 视频号 -->
        <div class="qr-card">
          <div class="qr-img-wrap">
            <img src="/images/qr-video-account.jpg" alt="帆哥AI研究室视频号" loading="lazy" />
          </div>
          <div class="qr-label">
            <strong>🎬 视频号</strong>
            <span>帆哥AI研究室</span>
          </div>
        </div>

        <!-- 抖音 -->
        <div class="qr-card">
          <div class="qr-img-wrap">
            <img src="/images/qr-douyin.png" alt="帆哥Benjamin@AI研究社" loading="lazy" />
          </div>
          <div class="qr-label">
            <strong>🎵 抖音</strong>
            <span>帆哥 Benjamin@AI 研究社</span>
          </div>
          <div class="douyin-id">抖音号：<strong>584401655</strong></div>
        </div>
      </div>

      <!-- 其他平台链接 -->
      <div class="social-links-row">
        <a href="https://space.bilibili.com/13472754" target="_blank" rel="noopener">📺 B站 · 帆哥Benjamin_AI研究院</a>
        <a href="https://www.xiaohongshu.com/user/profile/5c2b8221000000000701bb2d" target="_blank" rel="noopener">📕 小红书 · 帆哥Benjamin_AI 研究院</a>
        <a href="https://www.zhihu.com/people/fanshuai001" target="_blank" rel="noopener">💬 知乎 · 帆哥AI研究院</a>
      </div>
    </section>

    <!-- Subscribe -->
    <section class="subscribe-bar">
      <p>不想错过更新？订阅 <a href="/rss.xml">RSS Feed</a></p>
    </section>

  </div>
</BaseLayout>

<style>
  .about-page { max-width: 100%; }

  /* ── Hero + 头像 ── */
  .about-hero { padding: 0.5rem 0 2rem; }
  .hero-accent {
    width: 60px; height: 3px;
    background: linear-gradient(to right, var(--accent), transparent);
    margin: 0 auto 1.5rem; border-radius: 2px;
  }
  .hero-avatar-row {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    max-width: 42rem;
    margin: 0 auto;
  }
  .hero-avatar {
    width: 96px;
    height: 96px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid var(--accent);
    box-shadow: 0 4px 16px rgba(184,74,60,0.18);
    flex-shrink: 0;
    transition: transform 0.25s ease;
  }
  .hero-avatar:hover { transform: scale(1.06); }
  .hero-text { text-align: left; }
  .hero-text h1 { margin-bottom: 0.8rem; font-size: var(--fs-h1); }
  .lead {
    font-size: var(--fs-lead);
    color: var(--text-secondary);
    line-height: 1.7;
  }

  /* ── Card — 统一板式 ── */
  .card {
    background: var(--bg-card);
    border-radius: var(--radius);
    padding: 2.2rem 2.4rem;
    margin-top: 2rem;
    box-shadow: var(--shadow-sm);
    border: 1px solid rgba(0,0,0,0.04);
  }
  .card h2 { margin-top: 0; font-size: var(--fs-h2); }
  .card p { color: var(--text-secondary); font-size: var(--fs-body); }
  .card > p + p { margin-top: 1em; }

  /* ── Tool 分类标题 ── */
  .tool-intro {
    font-size: var(--fs-sm);
    color: var(--text-light);
    margin-bottom: 1.2rem;
    font-style: italic;
  }
  .tool-category {
    font-size: var(--fs-h3);
    font-weight: 600;
    color: var(--text-primary);
    margin: 2rem 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--rule);
    font-family: var(--serif-head);
  }

  /* ── Tool grid ── */
  .tool-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 0.9rem;
    margin-top: 1rem;
  }
  .tool-item {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding: 1.1rem 1.3rem;
    background: rgba(0,0,0,0.02);
    border-radius: 8px;
    border-left: 3px solid var(--accent);
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .tool-item:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow-sm);
  }
  .tool-item.featured {
    border-left-width: 4px;
    background: linear-gradient(135deg, rgba(184,74,60,0.04), rgba(184,74,60,0.01));
  }
  .tool-item strong {
    font-family: var(--serif-head);
    font-size: var(--fs-body);
    font-weight: 600;
    color: var(--text-primary);
  }
  .tool-item span {
    font-size: var(--fs-sm);
    color: var(--text-light);
    line-height: 1.55;
  }

  /* ── Clean list ── */
  .clean-list {
    list-style: none;
    padding: 0;
    margin: 1.2rem 0 0;
  }
  .clean-list li {
    padding: 0.8rem 0;
    border-bottom: 1px solid #f0ebe1;
    color: var(--text-secondary);
    line-height: 1.65;
    font-size: var(--fs-body);
  }
  .clean-list li:last-child { border-bottom: none; }
  .clean-list strong { color: var(--text-primary); }

  /* ── Philosophy ── */
  .philosophy blockquote {
    border-left: 4px solid var(--accent);
    margin: 1rem 0 1.5rem;
    padding: 1.2rem 1.5rem;
    background: linear-gradient(135deg, var(--accent-soft), transparent);
    border-radius: 0 var(--radius) var(--radius) 0);
    font-style: italic;
    font-size: var(--fs-lead);
    color: var(--text-primary);
    line-height: 1.6;
  }
  .philosophy cite {
    display: block;
    text-align: right;
    margin-top: 0.5rem;
    font-size: var(--fs-sm);
    color: var(--accent);
    font-style: normal;
  }

  /* ════════════════ QR 二维码区域 ════════════════ */
  .qr-zone { text-align: center; }
  .qr-intro {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    margin-bottom: 1.8rem;
  }
  .qr-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    margin-top: 1.5rem;
  }
  .qr-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.8rem;
    padding: 1.2rem 1rem;
    background: #fff;
    border-radius: 12px;
    border: 1px solid #ede8dc;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .qr-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.08);
  }
  .qr-img-wrap {
    width: 160px;
    height: 160px;
    border-radius: 10px;
    overflow: hidden;
    background: #f8f6f1;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #e8e2d4;
  }
  .qr-img-wrap img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }
  .qr-label {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    text-align: center;
  }
  .qr-label strong {
    font-family: var(--serif-head);
    font-size: var(--fs-body);
    font-weight: 600;
    color: var(--text-primary);
  }
  .qr-label span {
    font-size: var(--fs-xs);
    color: var(--text-light);
  }
  .douyin-id {
    margin-top: 0.2rem;
    font-size: var(--fs-sm);
    color: var(--accent);
    padding: 0.25rem 0.7rem;
    background: var(--accent-soft);
    border-radius: 20px;
  }
  .douyin-id strong {
    font-family: var(--sans);
    font-weight: 700;
    letter-spacing: 0.5px;
  }

  /* 其他平台文字链接行 */
  .social-links-row {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 1.5rem 2rem;
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px dashed var(--rule);
  }
  .social-links-row a {
    font-size: var(--fs-sm);
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.15s ease;
  }
  .social-links-row a:hover {
    color: var(--accent);
  }

  /* ── Subscribe ── */
  .subscribe-bar {
    text-align: center;
    padding: 2rem 1rem;
    margin-top: 2rem;
    font-size: var(--fs-sm);
    color: var(--text-secondary);
  }
  .subscribe-bar a {
    color: var(--accent);
    font-weight: 600;
    border-bottom: 1.5px solid var(--accent);
    padding-bottom: 2px;
  }

  /* ── 响应式 ── */
  @media (max-width: 900px) {
    .qr-grid { grid-template-columns: repeat(2, 1fr); }
    .hero-avatar-row { flex-direction: column; text-align: center; }
    .hero-text { text-align: center; }
  }

  @media (max-width: 700px) {
    .card { padding: 1.5rem 1.3rem; }
    .tool-grid { grid-template-columns: 1fr; }
    .qr-grid { grid-template-columns: 1fr; justify-items: center; }
    .qr-img-wrap { width: 180px; height: 180px; }
    .social-links-row { flex-direction: column; align-items: center; gap: 0.8rem; }
    .about-hero h1 { font-size: var(--fs-h1); }
    .lead { font-size: var(--fs-body); }
    .hero-avatar { width: 72px; height: 72px; }
  }
</style>
